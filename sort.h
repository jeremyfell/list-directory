// Handles sorting
#ifndef _SORT_H_
#define _SORT_H_

// Lexicographically sorts an array of strings in place
// Ignores any periods at the start of a string (to match ls behavior)
void Sort_lexicographicalSort(int length, char** strings);

#endif
